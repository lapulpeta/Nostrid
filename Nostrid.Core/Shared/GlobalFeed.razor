@using Nostrid.Model;
@using Nostrid.Data.Relays;

@inject AllSubscriptionFilterFactory allSubscriptionFilterFactory;

<FeedViewer BaseFilter="@filter" MessageKinds="@(new[]{NostrKind.Text, NostrKind.Repost, NostrKind.Badge})" />

@code {
    private SubscriptionFilter? filter;

    protected override void OnParametersSet()
    {
        filter = allSubscriptionFilterFactory.Create();
    }
}